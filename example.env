# 🌳 가족 트리 웹 애플리케이션 환경 변수 설정
# 이 파일을 복사하여 .env 파일로 만들고 실제 값으로 수정하세요.
# 명령어: cp example.env .env (Mac/Linux) 또는 copy example.env .env (Windows)

# ===================================
# 서버 설정
# ===================================

# 서버 포트 번호 (기본값: 3000)
PORT=3000

# 실행 환경 (development / production)
# development: 에러 스택 트레이스 표시, 디버그 모드
# production: 최적화된 성능, 에러 정보 최소화
NODE_ENV=development

# ===================================
# 데이터베이스 설정
# ===================================

# SQLite 데이터베이스 파일 경로 (기본값: ./data/familytree.db)
# DB_PATH=./data/familytree.db

# 데이터베이스 백업 디렉토리 (기본값: ./data/backups)
# BACKUP_DIR=./data/backups

# ===================================
# 파일 업로드 설정
# ===================================

# 업로드 파일 저장 디렉토리 (기본값: ./uploads)
# UPLOAD_DIR=./uploads

# 최대 파일 크기 (바이트 단위, 기본값: 5MB = 5242880)
# MAX_FILE_SIZE=5242880

# 허용할 이미지 파일 타입 (쉼표로 구분)
# ALLOWED_FILE_TYPES=image/jpeg,image/jpg,image/png,image/gif,image/webp

# ===================================
# CORS 설정
# ===================================

# CORS 허용 출처 (쉼표로 구분, 기본값: 모든 출처 허용)
# 프로덕션에서는 반드시 특정 도메인으로 제한하세요
# CORS_ORIGIN=http://localhost:3000,http://192.168.45.46:3000

# ===================================
# 보안 설정
# ===================================

# 세션 시크릿 키 (향후 인증 기능 추가 시 사용)
# SESSION_SECRET=your-secret-key-here

# JWT 시크릿 키 (향후 API 인증 추가 시 사용)
# JWT_SECRET=your-jwt-secret-here

# ===================================
# 로깅 설정
# ===================================

# 로그 레벨 (debug / info / warn / error)
# LOG_LEVEL=info

# 로그 파일 저장 여부 (true / false)
# ENABLE_FILE_LOGGING=false

# 로그 파일 경로 (기본값: ./logs)
# LOG_DIR=./logs

# ===================================
# 네트워크 설정
# ===================================

# 모바일 접속용 로컬 IP 주소 (자동 감지되므로 보통 설정 불필요)
# 수동으로 설정하려면 주석을 제거하고 실제 IP로 변경
# LOCAL_IP=192.168.45.46

# ===================================
# 기타 설정
# ===================================

# 타임존 설정 (기본값: Asia/Seoul)
# TZ=Asia/Seoul

# 디버그 모드 (true / false)
# DEBUG=false

